# dubbo client yaml configure file


check: true
# client
request_timeout : "3s"
# connect timeout
connect_timeout : "3s"

config_center:
  protocol: apollo
  address: 106.54.227.205:8080
  app_id: testApplication_yang
  namespace: dubbo_consumer
  config_file: dubbo_consumer
  app_config_file: dubbo_consumer
  cluster: dev

# application config
application:
  organization : "ikurento.com"
  name  : "BDTService"
  module : "dubbogo user-info client"
  version : "0.0.1"
  owner : "ZX"
  environment : "dev"
