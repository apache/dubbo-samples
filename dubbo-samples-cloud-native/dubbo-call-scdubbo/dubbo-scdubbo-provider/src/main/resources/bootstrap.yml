server:
  port: 8099

spring:
  application:
    name: scdubbo-provider
  cloud:
    consul:
      host: ${consul.address:127.0.0.1}
      port: 8500
      enabled: true

management:
  security:
    enabled: false

  endpoint:
    consul:
      enabled: false
  health:
    consul:
      enabled: false

endpoints:
  consul:
    enabled: false
